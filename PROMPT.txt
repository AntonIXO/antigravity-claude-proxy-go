You are building a Go/gRPC reimplementation of an Antigravity Cloud Code proxy.
Your working directory is /root/antigravity-go-proxy.

START HERE:
1. Read CLAUDE.md and PLAN.md in full before writing any code. PLAN.md is the
   authoritative phased roadmap — follow it in order.
2. Read the reference material it points to:
   - .reference/ (grpc-methods.txt, proto-messages.txt, agy-capture.pcap, proxy-capture.pcap)
   - /root/antigravity-claude-proxy/src/ (the Node proxy whose logic you port)
   - /root/hermes-claude-auth/anthropic_billing_bypass.py (masking philosophy)
   - the agy binary at /root/.local/bin/agy (cross-check constants with `strings`)

MISSION: make the proxy's network fingerprint indistinguishable from the real
`agy` CLI (a Go binary) so it doesn't trip Google's ToS-ban detection. The two
ban vectors are REST-vs-gRPC and Node-vs-Go TLS. You fix both by being a plain
Go gRPC client — exactly what agy is.

THE ONE RULE: never touch TLS internals. Empty tls.Config{} via grpc-go. No utls,
no custom CipherSuites/CurvePreferences/NextProtos. Go 1.26.4 default crypto/tls
reproduces agy's JA3/JA4 automatically; overriding anything breaks the disguise.

EXECUTION RULES:
- Work phase by phase (Phase 0→6 in PLAN.md). Each phase must compile and pass its
  gate before moving on. Commit after each phase with a clear message.
- Phase 2's fingerprint gate is the make-or-break test: capture your client's TLS
  ClientHello with tcpdump and confirm `tls.handshake.ja4` equals agy's
  `t13d1312h2...` (see .reference/agy-capture.pcap). Do not build further until it matches.
- Recover the v1internal.CloudCode .proto from the agy binary with protodump —
  NEVER fabricate a schema. If it's genuinely unrecoverable, STOP and report;
  a wrong schema fails silently and looks like a ban.
- Port the Node proxy's format-conversion, backoff, rotation, and account logic
  faithfully — translate, don't redesign.
- Use NEW port 8091 and a NEW systemd unit. Do not disturb the running Node proxy
  on :8090 or its accounts.json until the user switches over.
- Tools you'll need aren't all installed: `pacman -S protobuf` and
  `go install github.com/arkadiyt/protodump/cmd/protodump@latest` and
  `go install google.golang.org/protobuf/cmd/protoc-gen-go@latest` and
  `.../protoc-gen-go-grpc@latest` in Phase 0.
- Verify your work with real tool output (compile, curl, tcpdump/tshark). Do not
  claim a phase passed without running its gate.

When done, confirm every checkbox in PLAN.md "Acceptance", especially the
packet-verified JA4 match, and summarize what was built and how to switch Hermes over.
